"use client";

import React from "react";
import ReactShowMoreText, {
  ReactShowMoreTextProps,
} from "react-show-more-text";

export const ShowMoreText: React.FC<ReactShowMoreTextProps> = (props) => {
  return <ReactShowMoreText {...props}></ReactShowMoreText>;
};
